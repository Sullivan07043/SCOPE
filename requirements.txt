# SCOPE Router Dependencies

# Core dependencies
numpy>=1.21.0
tqdm>=4.62.0

# Embedding model (for similarity computation)
sentence-transformers>=2.7.0
transformers>=4.51.0
torch>=2.0.0

# HuggingFace datasets
datasets>=2.14.0
huggingface-hub>=0.16.0

# VLLM (for local SCOPE model inference)
vllm>=0.4.0

# HTTP requests (for OpenRouter API in custom model pool)
requests>=2.28.0
